---
kind: pipeline
type: docker
name: Build ARM

platform:
  os: linux
  arch: arm64
  
steps:
  - name: Ubuntu 20.04
    image: ubuntu:focal
    commands:
    - ./src/hst_autocompile.sh --hestia --install --debug '~localsrc'
  